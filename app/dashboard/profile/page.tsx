"use client";

import React from "react";
import { useEffect } from "react";

const ProfilePage = () => {
    useEffect(() => {
        // Fetch user data or some logic
    }, []);

    return (
        <div>
            <h1>Profile Page</h1>
            {/* User profile details go here */}
        </div>
    );
};

export default ProfilePage;